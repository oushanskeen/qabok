
Solution: 
``` javascript
/**
* @param {number[]} digits
* @return {number[]}
*/

var plusOne = (digits) => {
	let i = digits.length - 1
	while(i > -1){
		if(digits[i] < 9){
			digits[i]++
			return digits
		}else{
			digits[i] = 0
		}
		i--
	}
	digits.unshift(1)
	return digits
}
```

